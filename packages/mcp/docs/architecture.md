# Suzaku MCP + Telegram Bots — Architecture Overview

*Snapshot: 2026-07-09, branch `safe-propose`. Deep reference: `packages/mcp/CLAUDE.md`; deploy runbooks: `packages/mcp/deploy/openclaw/README.md`.*

## The idea in one paragraph

Clients (Dexalot first) talk to a live LLM session in a Telegram group. The session's brain is OpenClaw (the Telegram-bot agent framework, formerly clawdbot); its hands are the **Suzaku MCP server** (MCP = Model Context Protocol), which shells out to `suzaku-cli` for every on-chain read/write. Capability is split across **three bots** with escalating power — read-only monitor → Safe-proposal drafter → single-permissionless-write cache bot — each locked to a minimal MCP *profile* (tools that aren't in the profile are never registered, not merely blocked). All three are services of one `docker-compose.yml` built from one image.

## 1. Branch & PR state (2026-07-09)

```
origin/main (151b0d1) ─ keeper now IN main (PR #65 merged 2026-06-12)
   │
   └─ mcp == origin/mcp (PR #50 open: mcp → main, ~101 commits)
        │   MCP server + heartbeat + all safe-propose work (merged in June 11)
        │
        └─ safe-propose
              +2 commits: 83a59f6 (public stake cache bot), fd563ba (deploy docs)
```

**Merge note:** a trial merge of `origin/main` into this branch produces **11 conflict hunks in `pnpm-lock.yaml` and `src/lib/cliParser.ts`**; `src/cli.ts`, `src/lib/viemUtils.ts`, `src/uptime.ts`, `.dockerignore` were changed by both sides but auto-merge. The cliParser conflict is *semantic*, not just textual: keeper's main added an env check to the old in-parser mainnet guard (`process.argv.includes('mainnet') || process.env.NETWORK === 'mainnet'` → throw), while this branch deliberately **removed all policy from cliParser** and rebuilt the guard as a `preAction` hook in `cli.ts` (with the two exemptions below). Resolution should keep the preAction design and consciously port keeper's `NETWORK`-env intent into it.

**Keeper relationship:** keeper source lives in main (`packages/keeper/`) since PR #65; it is a separate automation stack (StakingVault epoch processing / harvests / two-phase completions) and is not part of the MCP bot deployment.

## 2. Runtime topology

```
Telegram ops group (ID via TELEGRAM_GROUP_ID)       Admin DMs (allowlist by numeric user ID)
        │ @mention required                                │
        ▼                                                  ▼
┌─ Docker host (currently a dev workstation; VPS migration planned) ───────────┐
│                                                                              │
│  suzaku-bot  (read-only monitor — LIVE)                                      │
│  ├─ OpenClaw 2026.6.5 (pinned tag+digest) gateway                            │
│  │   ├─ LLM primary: openai/gpt-5.5 via Codex plugin                         │
│  │   │    (ChatGPT-subscription OAuth; profile persisted in openclaw-state   │
│  │   │     volume, survives rebuilds)                                        │
│  │   ├─ LLM fallback: anthropic/claude-sonnet-4-6 (ANTHROPIC_API_KEY)        │
│  │   ├─ cron scheduler (sqlite in volume): heartbeat-alerts (every 4 h),     │
│  │   │    heartbeat-digest (once per 3.5-day epoch)                          │
│  │   └─ workspace: SOUL.md (persona + pinned Dexalot addresses), EPOCHS.md   │
│  │                                                                           │
│  ├─ MCP wiring — TWO paths to the SAME server:                               │
│  │   • Codex runtime: native MCP registration in codex config.toml,          │
│  │     regenerated by entrypoint.sh on every start                           │
│  │   • Anthropic fallback: mcporter CLI bridge (mcporter.json)               │
│  │                                                                           │
│  └─ Suzaku MCP server --read-only (stdio subprocess)                         │
│       └─ suzaku-cli subprocess per tool call (--json --yes,                  │
│            8-var env allowlist) ──► Avalanche C-Chain / P-Chain RPC          │
│                                                                              │
│  suzaku-propose-bot  (compose profile "propose" — NOT deployed)              │
│  suzaku-cache-bot    (compose profile "cache"   — NOT deployed)              │
│                                                                              │
│  hardening (all 3): cap_drop ALL · no-new-privileges · pids 256 · mem 2g     │
│  tmpfs /tmp · restart unless-stopped · iptables SSRF backstop on br-suzaku   │
└──────────────────────────────────────────────────────────────────────────────┘
```

Sessions: OpenClaw keeps one session per Telegram chat/thread; a thread's model/runtime/tool config is computed once at session start, so after any config change you send `/new` in the chat. Cron jobs run in isolated sessions.

## 3. The three bots

| | `suzaku-bot` (monitor) | `suzaku-propose-bot` | `suzaku-cache-bot` |
|---|---|---|---|
| Chat surface | group @mention + admin DM | **DM-only** (`groupPolicy: deny`) | group @mention (same group as monitor by default) |
| MCP profile | `--read-only` (69 read tools) | `--propose-only` (69 + 2 propose tools) | `--public-write` (69 + `middleware_cache_stakes`) |
| Can change chain state | no | **no** — queues off-chain Safe proposals only | yes — exactly one permissionless tx type |
| Key material | **no signing key** (holds only LLM/Telegram creds + optional Snowscan explorer key) | Safe **delegate** key + Safe API key (file secrets) | fresh role-less EOA, tiny gas balance (file secret) |
| LLM | gpt-5.5 (Codex) → sonnet-4-6 fallback | sonnet-4-6 only (mcporter path only) | sonnet-4-6 only (mcporter path only) |
| Cron | yes (heartbeat) | no | no |
| SOUL file | `SOUL.md` | `SOUL-propose.md` | `SOUL-cache.md` |
| Audit volume | `audit-data` | `audit-data-propose` | `audit-data-cache` |
| Status | **live** | built, awaiting go-live prereqs | dark launch pending |

**LLM auth is per-bot config, not per-bot architecture.** All three run the same OpenClaw image; the model is one config field (`agents.defaults.model.primary`). Only the monitor has the subscription (Codex OAuth) route wired: it alone mounts the `openclaw-state` volume (OAuth persistence) and gets the native Codex MCP registration from `entrypoint.sh` — which identifies the propose/cache bots by their mcporter template mount and deliberately skips them. All three containers receive the same shared `ANTHROPIC_API_KEY`, so the API route works everywhere; per-bot cost attribution is impossible until each bot gets its own key.

There is also a fourth artifact: a **standalone distroless MCP image target** (`--read-only`, no shell) for plain MCP clients, and a local (untracked) repo-root `.mcp.json` can attach the **full 127-tool profile** to dev sessions (mainnet writes come back as suggestions, not executions).

## 4. Security model in one screen

- **Profile = registration surface.** Excluded write tools never appear in `tools/list` (asserted by tests), so a prompt-injected model can't even attempt them.
- **Write guard chain** (full profile): require signer → deny/allow lists → per-tx value limit → optional elicitation → **network matrix: mainnet never auto-executes** (returns a suggested command instead; testnet executes).
- **Two deliberate, narrow exceptions** to "mainnet software keys are blocked" (both enforced in the `cli.ts` preAction guard):
  1. `--safe` + `--safe-propose` on `rewards set-amount|distribute` — off-chain proposal only; the CLI *refuses Safe owner keys*, so the bot key can queue but never execute. Humans sign the decoded calldata in the Safe UI.
  2. `--public-call` on `middleware calc-operator-cache` — the one real tx the cache bot may send; `runPublicCacheCli()` accepts exactly one command shape, middleware + network are pinned by env, `rpcUrl` rejected.
- **Secrets as compose file secrets** (`/run/secrets/…`), read at spawn time — never in `docker inspect`, `/proc/*/environ`, or rendered configs. Telegram/Anthropic keys never propagate to CLI subprocesses (8-variable env allowlist).
- **Cache bot off-switch is deny-by-default**: compose uses `${SUZAKU_CACHE_DENY_TOOLS-middleware_cache_stakes}` (single-dash). Footgun to remember: to *enable* writes set the var to an **empty value** in `.env`; deleting the line re-blocks.
- Rate limit + concurrency caps per server; JSONL audit log per bot volume (rotated); `SUZAKU_MCP_PUBLIC_HEALTH` hides signer/guard info from `health_check` on public bots; SSRF blocklist on user-supplied RPC URLs plus host iptables backstop; OpenClaw pinned ≥ 2026.4.22 (sandbox-escape advisories floor).

## 5. Key material

| Key | Where | Can | Cannot |
|---|---|---|---|
| Monitor bot | no *signing* key; env carries `ANTHROPIC_API_KEY`, Telegram token, gateway token, optional `SNOWSCAN_API_KEY` | read chain (unauthenticated RPC; the Snowscan explorer key only speeds event scans ~45 s → ~5 s) | sign or send anything |
| Safe delegate EOA | `./secrets/delegate_pk` → `/run/secrets` | sign EIP-712 proposal payloads (needs no AVAX) | execute; owner-key use refused by CLI |
| Safe API key | `./secrets/safe_api_key` | authenticate to Safe tx service (mainnet) | move funds |
| Cache EOA | `./secrets/cache_pk` | send `calcAndCacheStakes` (value-neutral, deterministic), spend its tiny gas balance | any role-gated call; blast radius ≈ its AVAX balance |
| Rewards **Safe** (multisig) | owners' wallets, not in this system | execute proposals after owner signatures | — |

## 6. The two write flows

**Propose flow (rewards):** DM "prepare 10 450 ALOT for epoch 46" → bot pre-checks with fresh reads (epoch window `currentEpoch-2 ≤ e < currentEpoch`, no prior set-amount **accumulation** — the epoch-35/36 incident class, amount `< SUZAKU_MAX_REWARDS_AMOUNT`, no duplicate already pending in the queue) → CLI `--safe --safe-propose` builds **one atomic MultiSend batch** (`approve` + `setRewardsAmountForEpochs` — must be atomic because the contract does `transferFrom` at set time) with a pinned nonce → proposal appears in the Safe queue → owners re-verify decoded calldata and sign → Safe executes. Queue check is fail-open by design; the human signature is the hard gate.

**Cache flow (stakes):** group member asks for the epoch stake cache → tool fresh-reads `cacheByClass[class]` (skips if already cached) → CLI `middleware calc-operator-cache <mw> <epoch> <class> --public-call` sends the tx → fresh post-read confirms. Rollout ladder is mandatory: dark launch (deny-on, unfunded) → fuji staging → mainnet unfunded (must fail only on gas) → fund small, never auto-top-up, `deployment_heartbeat` alerts below `SUZAKU_CACHE_KEY_MIN_AVAX`.

## 7. Operational snapshot (2026-07-09)

| Thing | State |
|---|---|
| `suzaku-bot` container | **up, healthy** — the read-only monitor, on a dev workstation (VPS migration pending) |
| Propose / cache bot containers | not deployed yet |
| Heartbeat crons | registered in the `openclaw-state` volume (June 10, live-verified); alerts every 4 h, digest per epoch |
| Keeper stack (separate from MCP) | `keeper-core` crash-looping — connection refused to its configured avalanchego RPC (node unreachable); Grafana up; Prometheus exited |
| MCP test suite | 247 passing at the June 15 audit of `83a59f6` (HEAD adds docs only) |

**Observability gap (known):** the per-bot audit JSONL records `duration_ms`, tool, args, network, success, and signer method per CLI call — raw telemetry, never analyzed. There is **no** cost/token tracking, no quality evals (all 247 tests are deterministic, no LLM in the loop), no end-to-end latency measurement, and no per-key/account attribution (one shared `ANTHROPIC_API_KEY` + one Codex account across bots).

## 8. Outstanding work, in order

1. ~~Push the cache-bot commits to `origin/mcp`~~ (done with this commit).
2. **Reconcile PR #50 with the new main** (keeper merged): 11 conflict hunks in `pnpm-lock.yaml` + `src/lib/cliParser.ts`; keep the preAction guard design, port keeper's `NETWORK`-env check into it deliberately; re-run the guard live-verification afterwards.
3. **Review** of the one security-sensitive relaxation (`--safe-propose` guard exemption) — still pending.
4. **Propose-bot go-live prereqs:** rewards Safe address (set-amount is still sent by an EOA today), grant the Safe `REWARDS_MANAGER_ROLE` + fund ALOT, register the delegate, probe the Ash fuji tx-service delegates endpoint, fuji E2E (`--safe` doesn't work on anvil).
5. **Cache-bot dark-launch ladder** (§6); set `SUZAKU_CACHE_KEY_ADDRESS` *before* funding or the balance alert silently skips.
6. **Keeper-core RPC outage** — point it at a live node or stop the restart loop.
7. Parked: VPS migration, KMS signing (delegate key never on host — design noted in `packages/mcp/CLAUDE.md`), CLI `PK_PCHAIN` gap (separate P-Chain key currently ignored).

## 9. Where things live

| Path | What |
|---|---|
| `packages/mcp/src/server.ts` | MCP entry; profile flags `--read-only` / `--propose-only` / `--public-write` |
| `packages/mcp/src/cli-runner.ts` | subprocess engine, suggest/confirm matrix, env allowlist, audit, rate limits |
| `packages/mcp/src/guard.ts` | deny/allow, value limits, elicitation |
| `packages/mcp/src/tools/` | 14 tool files (127 tools) + `heartbeat.ts` |
| `packages/mcp/deploy/openclaw/` | Dockerfile (build → `standalone` / `bot` targets), compose (3 services), entrypoint, per-bot `openclaw*.json` + `mcporter*.json` + `SOUL*.md`, iptables script, README (runbooks: setup, Codex OAuth login, propose/cache rollout, incident response) |
| `packages/mcp/docs/` | `heartbeat-design.md`, `public-cache-writes-plan.md`, `architecture-review-2026-06.md` |
| `scripts/add-safe-delegate.mjs` | one-time delegate registration (owner action) |
| `packages/mcp/CLAUDE.md` | authoritative deep reference (safety model, env vars, invariants) |
